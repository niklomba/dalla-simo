name: Genera index completo Dalla Simo

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  genera-index:
    if: ${{ !contains(github.event.head_commit.message, '[generated-index]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Scarica repository
        uses: actions/checkout@v4

      - name: Ricompone l'app completa
        shell: bash
        run: |
          set -euo pipefail
          cat app.part01 app.part02 app.part03 app.part04 app.part05 app.part06 app.part07 app.part08 app.part09 app.part10 app.part11 | tr -d '\n\r ' | base64 -d | gzip -d > index.html
          test -s index.html
          test "$(wc -c < index.html)" -gt 200000
          grep -q "Dalla Simo" index.html
          grep -qi "<!doctype html" index.html
          echo "INDEX_OK bytes=$(wc -c < index.html)"

      - name: Pubblica index.html completo su main
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "index.html già aggiornato"
            exit 0
          fi
          git commit -m "[generated-index] Pubblica Dalla Simo completa"
          git push
