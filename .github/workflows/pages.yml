name: Manutenzione Dalla Simo

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verifica:
    runs-on: ubuntu-latest
    steps:
      - name: Verifica repository
        run: echo "Dalla Simo viene pubblicata direttamente dal branch main tramite GitHub Pages."
